# Deployment configuration
# Copy to deployment.config and fill in your values.
# deployment.config is gitignored - never commit real IPs or secrets.

# EC2 IP addresses
# EC2_ELASTIC_IP is preferred (static, survives restarts)
# EC2_IP is the dynamic Public IPv4 (changes on restart)
EC2_IP=54.123.45.67
EC2_ELASTIC_IP=

# Domain (optional - for nginx and CORS config)
DOMAIN=yourdomain.com

# Use HTTPS for API URL when building frontend (set true after SSL is set up)
USE_HTTPS=false

# Email for Let's Encrypt (used by setup-ssl.ps1)
SSL_EMAIL=your@email.com

# GitHub repo URL - REQUIRED for deployment (uses git pull to sync code)
# Example: https://github.com/yourusername/your-repo.git
GITHUB_URL=

# Branch to pull (default: main)
GITHUB_BRANCH=main

# SSH key (path relative to project root)
KEY_PATH=security/counter-app-key.pem

# Codemagic / TestFlight (for scripts/update-codemagic-config.ps1)
# APP_ID = Bundle ID (e.g. com.yourdomain.yourapp)
APP_ID=com.yourdomain.counterapp
# Email for Codemagic build notifications
CODEMAGIC_EMAIL=your@email.com
